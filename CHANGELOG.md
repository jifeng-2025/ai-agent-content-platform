# 更新记录

## 2026-09-11 开发候选更新

- 默认创作改为主题、标题选择、文字模型和1–5张配图、大纲确认、流式图文、末尾建议评审。
- 网页管理员模型设置，DeepSeek/百炼文字与demo/Gemini/豆包配图；本地图片和主密钥持久化。
- 单图修改提示词重试、单独重试审稿建议；保留恢复与预算保护。
- 修复历史列表和详情导出，提供图文ZIP、排版HTML和单文件MD；导航首页置前，模型设置移至数据之后。
- 最新导出29项专项通过，离线桌面/手机样例已检查。各阶段结果独立，不宣称最终完整A4或真实API全部通过。
- 用户本轮明确授权提交推送源码；不打标签、不创建Release、不部署。历史条目中的“未提交推送”指当时状态。

## 未发布：确认大纲后直接出图文

- 默认创作恢复流式正文，配图显示纯旋转圆环（无百分比）。
- 仅保留大纲确认；图文后评审一次，只给建议，不阻断或自动修订。
- 复用持久任务、预算、本地存图与导出；旧分步路径保留。功能与真实调用待用户验收。

## 未发布：管理员模型设置

- 网页配置文字/图片默认模型，保留百炼，新增兼容聊天接入DeepSeek/方舟；图片复用Gemini/豆包/demo。
- 加密Key与独立主密钥卷；任务固定配置版本；同源管理员/CSRF与公开HTTPS端点限制。
- 功能、浏览器与真实调用由用户验证；不标记A4全部通过。

# Changelog

## 双供应商候选 — 2026-09-11（剩余验收由用户执行，未发布）

- 默认DEMO不变；Gemini原生协议与方舟Seedream图片协议独立适配，任选一家配置。
- 创建时持久固定provider/model/规格；本地图片留存与元数据、错误分类、无自动收费切换。
- 前后端配置检查、单供应商选择、来源/usage展示；新增两张兼容表。
- 新增协议/配置/HTTP/快照/Runtime测试，Docker已恢复，已执行专项验证；用户接手安装、页面、升级和真实体验。真实API全部NOT_RUN，随后由用户手动体验。


## v0.1.0 演示候选 — 2026-09-10（未发布）

- A1：严格ReviewResult、最多两次局部修订与人工出口，保留正文历史。
- A2：评审版本/差异、人工接受、编辑重评、单图重试、归属和版本冲突保护。
- A3：持久调度、租约/fence、检查点、外部调用账本、取消/预算及SSE恢复。
- A4：本地鉴权存图、生成结果留存和离线ZIP；COS仅可选，不迁移旧图。
- 基础演示只需百炼文字Key。DEMO在模型调用前分流，受限Picsum失败时绘制PNG，标记演示/占位、非AI、无语义匹配保证；Gemini可选，未配置则前后端禁止使用。
- 生产运行镜像补齐字体和非root图片目录权限；随机数据库密码初始化与配置保留。
- 251文件新候选同版本工程门禁通过：113项Java功能测试、12项真实进程故障、177项浏览器检查、44张截图实看，PNG卷重建/离线ZIP/新装升级和回退通过。

真实文字NOT_RUN；真实AI生图DEFERRED_BY_USER / NOT_RUN；分发许可范围待确认。既有contextLoads配置失败和lint16错误1警告保留。完整证据见artifacts/a4/report.md。

247文件双Key本地存储候选的102项Java/143项浏览器结果仅为历史，保留artifacts/a4/pre-demo；更早236文件候选在pre-local-storage，均未替代本次验收。

未提交、推送、打标签或发布；B/H1/D1未实现。
