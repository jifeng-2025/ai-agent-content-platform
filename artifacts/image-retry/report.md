# 配图保存失败与提示词重试

本次用户任务已取得豆包返回的JPEG并持久缓存，失败在本地PNG转换后超5MiB；只读账本和离线ImageIO复现见diagnosis.json。已缓存600777字节JPEG（2048×2048），PNG为6520847字节，图片卷可写。这证明该次接口返回图片，不代表持续可用或实际账单核验；本轮新增收费请求0。官方文档抓取失败，未据第三方资料变更供应商协议。

修复：保持输入大小/格式/像素/路径校验及5MiB上限，PNG转换膨胀时最多6次等比例缩小后保存；可降低保存分辨率。新增配图卡片、错误提示、提示词修改及明确收费确认。POST /article/quick/{id}/image-retry 复用持久Runtime，校验登录/归属/状态版本/请求去重；QUICK_IMAGE_RETRY受原调用、重试及费用预算限制，保留正文/其他配图/评审及固定供应商配置。相同提示词优先复用生成缓存，改变提示词视为主动生成，不自动跨供应商或重发不确定请求。旧的原人工操作入口兼容。

不修改用户库或容器，不自动恢复本次图片。用户更新 backend/frontend 后，在配图卡片点击重试，保持原提示词确认，可恢复已缓存图片；修改提示词会产生新的收费请求。无新增SQL。

验证见compile.json、typecheck.json、tests.json和tests.log。未执行网页/完整前端build/新付费API/完整A4门禁，交由用户实战；不沿用旧PASS。

首次专项36项中35通过、1失败：旧A4ImageProviderTest未注入新增ModelSettings导致NPE；已补Mock初始化，未改变断言。first-tests.log保留。最终重跑结果见tests.log。

最终：后端package PASS；前端vue-tsc PASS；36项专项测试全部通过（存储10、转换超限1、供应商25），无跳过。未执行新重试UI/接口的完整联调，交由用户实战。
