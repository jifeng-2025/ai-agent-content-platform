# 演示候选：真实百炼文字冒烟（NOT_RUN）

用户明确调整MVP：基础演示只需DASHSCOPE_API_KEY；DEMO配图不调用Gemini，图片Key/COS凭据均非必填。真实AI生图状态为DEFERRED_BY_USER / NOT_RUN，本次不申请Gemini收费授权。未授权时不调用百炼，配置存在不是许可，不在聊天粘贴Key。

拟用一份500–700字短文“周末散步中的小发现”，一个章节、一张程序PNG演示图。百炼qwen-plus非思考模式；正常4次文字（标题、大纲、正文、评审），最多两次修订+重评共8次文字。演示图片计划由程序生成，0次Gemini请求；不自动开新轮次、换模型或重试不确定收费结果。

程序限制计划：max-calls=9（8次文字加1次演示步骤），max-image-retries=0，text-reserve-micros=10000、max-estimated-cost-micros=80000，演示图片预留0；结合持久预算使第9次文字调用被拒绝。每次文字max-tokens=2000；预计输入每次不超过6000 token（估算而非硬输入上限）。执行前以免费限制测试确认生产配置实际绑定；模型调用不支持跨供应商exactly-once。若超限/不确定则暂停，不增额或自动收费重试。

2026-09-10重新查看[百炼官方价格](https://help.aliyun.com/zh/model-studio/model-pricing)：qwen-plus低输入档非思考，全球输入0.8元/百万、输出2元/百万；国际输入2.936元/百万、输出8.807元/百万。8×6000输入+8×2000输出约0.0704元（全球）或0.2818元（国际）。建议授权预算人民币1元、一个样本、最多8次文字请求；按实际账户地区核实。此预算是执行约束与估算，不是供应商硬性账单封顶；实际usage和账单未知保持UNKNOWN/null。

使用最终验收JAR，不加载Mock供应商插件，独立MySQL/Redis/图片卷；从页面创建至评审、演示图本地保存、查看、Markdown/ZIP导出。演示图不得宣称语义匹配，实际检查其占位标记。必要人工接受需用户真实确认。任何业务代码更改都重新冻结并重跑门禁。

免费验收完成后只需用户一次确认：是否授权上述单样本文字调用，以及本机Key的配置位置/账户地区（不要发Key）。许可范围另需权利人许可证原文或书面允许公开修改后源码的依据。当前均未授权，NOT_RUN。
