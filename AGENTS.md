# 项目协作入口

本项目已选 B，先交付 A 的最小闭环。开始任务时：
1. 读取根目录 [agent.md](agent.md)（短索引）。
2. 读取 [docs/agent/status.md](docs/agent/status.md)。
3. 按任务编号只读取索引指定的主模块；跨模块依赖才补读，不批量加载 docs/agent 全文。
4. 检查 Git 状态及适用的更深层目录指令，再执行当前用户授权的任务。
5. 交付必要验证证据，只更新状态索引和受影响模块。

保护现有数据、用户改动与上游署名；不输出或提交密钥。不把规划/Mock/框架能力写成已实现成果。
